# Make를 활용한 설문 결과 이메일 자동 발송 구축 가이드

> 구글 폼 · 구글 시트 · Make · 지메일을 연동한 업무 자동화(RPA) 가이드북

---

## 📅 단계별 요약표

| 단계 | 작업 항목 | 사용 도구 | 주요 목적 |
| :--- | :--- | :--- | :--- |
| **1단계** | 구글폼 제작 | Google Forms | 설문 및 응답 데이터 수집 환경 구축 |
| **2단계** | 구글시트 연동 | Google Sheets | 수집된 데이터의 DB화 및 매핑 준비 |
| **3단계** | 테스트 응답 제출 | Web Browser | 자동화 시나리오 매핑용 가상 데이터 생성 |
| **4단계** | Make 시나리오 설계 | Make.com | 시트-라우터-지메일 모듈 배치 및 연결 |
| **5단계** | 모듈/필터 세부 설정 | Make.com | 점수 분기 조건(>=85, <85) 및 메일 템플릿 작성 |
| **6단계** | 시나리오 실행/테스트 | Make.com | Run once를 통한 자동화 흐름 검증 |
| **7단계** | 결과 확인 및 적용 | Gmail | 합격/불합격 이메일 자동 발송 검증 |

---

## 📝 상세 가이드

### 1단계: 구글폼(Google Forms) 만들기
1. 구글 드라이브 접속 후 **[신규] -> [Google 설문지]**를 생성합니다.
2. 설문 항목 구성:
   - **이름** (단답형, 필수)
   - **이메일 주소** (단답형, 필수 - 메일 발송용)
   - **영어 점수 또는 평가 항목** (숫자/객관식 항목)

> 💡 **핵심 팁 & 주의사항**  
> 이메일 항목 설정 시 **'이메일 주소 유효성 검사'**를 활성화하면 잘못된 주소 입력으로 인한 메일 발송 오류를 예방할 수 있습니다.

---

### 2단계: 구글시트를 구글폼에 맞춰 연동하기
1. 구글폼 상단의 **[응답]** 탭으로 이동합니다.
2. **[Sheets에 연결]** 버튼을 클릭하여 새 스프레드시트를 생성합니다 (예: `'설문지 응답 시트1'`).
3. 생성된 구글 시트에 A열(타임스탬프), B열(이름), C열(이메일), D열(영어점수) 등의 헤더가 자동으로 생성되었는지 확인합니다.

---

### 3단계: 설문 URL 복사 및 테스트 응답 제출
1. 구글폼 우측 상단 **[보내기] -> [링크 복사]**를 통해 설문지 URL을 복사합니다.
2. 새 브라우저 창에서 링크를 열고, 테스트 응답 데이터 2건 이상을 제출합니다:
   - **테스트 1**: 85점 이상 (합격 테스트용)
   - **테스트 2**: 85점 미만 (불합격 테스트용)

> 💡 **핵심 팁 & 주의사항**  
> Make에서 모듈 설정을 매핑할 때 실제 데이터 값이 있어야 항목 태그(이름, 이메일, 점수 등)를 손쉽게 선택할 수 있으므로 테스트 응답 제출은 필수 과정입니다.

---

### 4단계: Make 시나리오 생성 및 모듈 배치
1. Make(make.com) 로그인 후 **[Create a new scenario]**를 클릭합니다.
2. **첫 번째 모듈**: `Google Sheets` 모듈을 선택하고 `'Watch Rows'` 또는 `'Search Rows'`를 지정합니다.
3. **두 번째 모듈**: `Router` (Flow Control) 모듈을 추가하여 경로를 2개로 분기합니다.
4. **세 번째/네 번째 모듈**: 라우터 상단과 하단 끝에 각각 `'Gmail (Send an Email)'` 모듈을 연결합니다.

---

### 5단계: 모듈 설정 및 분기 조건(Filter) 세팅

#### 1) Google Sheets 모듈 설정
- **Spreadsheet ID**:
  - 방법 1) Dropdown에서 파일 선택
  - 방법 2) URL 주소창의 `/d/`와 `/edit` 사이의 ID 입력
- **Sheet Name**: `'설문지 응답 시트1'` 선택
- **Table contains headers**: `Yes` 선택 (A1:Z1 범위 확인)

#### 2) Router 필터(Filter) 설정 (★ 핵심)
- **상단 경로 필터 (1st: 85점 이상 합격)**:
  - **Condition**: 구글 시트의 `'영어점수'` 변수 선택
  - **Operator**: `Numeric operators` 섹션의 `'Greater than or equal to'` (`>=`) 선택
  - **Value**: `85` 입력
- **하단 경로 필터 (2nd: 85점 미만 불합격)**:
  - **Condition**: `'영어점수'` 변수 선택
  - **Operator**: `Numeric operators` 섹션의 `'Less than'` (`<`) 선택
  - **Value**: `85` 입력

#### 3) Gmail 모듈 설정
- **To**: 구글 시트의 `'이메일 주소'` 변수 매핑
- **Subject**: `[합격 안내]` 또는 `[불합격 안내]` 제목 작성
- **Content**: 이름 변수를 활용한 개인화 동적 본문 작성 (예: `'안녕하세요 {{이름}}님, 축하합니다...'`)

> 💡 **핵심 팁 & 주의사항**  
> 필터 설정 시 문자 연산자(`Text operators: Equal to` 등)가 아닌 **숫자 연산자(`Numeric operators: >=, <`)**를 사용해야 85점 판별이 정확하게 이루어집니다.

---

### 6단계: 시나리오 Run 및 테스트
1. Google Sheets 모듈 우클릭 -> **[Choose where to start] -> [All]** 선택 (기존 응답 데이터 포함 실행).
2. 좌측 하단의 **[Run once]** 버튼을 클릭하여 시나리오를 실행합니다.
3. 모듈과 라우터 사이의 점선에 초록색 숫자가 표시되며 데이터 이동 경로를 확인합니다.

---

### 7단계: 결과 확인 및 자동화 상시 가동
1. 지메일 수신함으로 이동하여 조건별(합격/불합격) 안내 메일이 올바르게 수신되었는지 확인합니다.
2. 테스트 성공 후 Make 화면 좌측 하단의 **[SCHEDULING]** 스위치를 **'ON'**으로 변경하여 폼 응답 시 즉시 자동 발송되도록 가동합니다.
